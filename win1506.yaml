---
- name: Verify WinRM service is running
  host: all
  win_service:
    name: WinRM
    state: started
  register: winrm_service_result
  failed_when: winrm_service_result.status != 'running'
  changed_when: false
